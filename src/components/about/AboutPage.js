import React from "react";

const AboutPage = () => (
  <div>
    <h2>Acerca</h2>
    <p>
      Esta aplicación utiliza React, Redux, React Router y otras librerias
      útiles.
    </p>
  </div>
);

export default AboutPage;
